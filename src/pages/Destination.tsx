export default function Destination({ location }: { location: string }) {
    return (
        <div className="min-h-screen pt-20 flex flex-col items-center justify-center">
            <h1 className="text-4xl font-bold">{location} Destination Stub</h1>
        </div>
    );
}
